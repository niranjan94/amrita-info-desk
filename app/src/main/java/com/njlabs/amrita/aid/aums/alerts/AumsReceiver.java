package com.njlabs.amrita.aid.aums.alerts;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/**
 * Created by Niranjan on 22-10-2014.
 */
public class AumsReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
        /*Ln.d("Starting Daily Update Service");
        Intent aumsUpdater = new Intent(context, AumsService.class);
        context.startService(aumsUpdater);
        Ln.d("Started Daily Update Service");*/
    }
}
